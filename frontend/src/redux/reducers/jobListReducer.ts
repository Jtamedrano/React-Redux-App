const initialState = {
  isFetchingApi: false,
};

export const jobListReducer = (
  state = initialState,
  action: { type: number }
) => {
  switch (action.type) {
    default:
      return state;
  }
};
